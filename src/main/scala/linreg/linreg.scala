package ai.economicdatasciences.linreg

import org.apache.spark._
import org.apache.spark.sql._

import ai.economicdatasciences.init.SparkInit.spark
import spark.implicits._

object LinReg {
    def main(args: Array[String]): Unit = {
        // val spark
    }
}
